#include <stdio.h>
#include <math.h>

int main(int argc, const char * argv[]) {
    
    double angle = 1;
    double sine = sin(angle);
    printf("The sine of %f radian is %.3f\n", angle, sine);
    return 0;
}
